[project]
name = "devin-superset-automation"
version = "0.1.0"
description = "Event-driven Devin remediation controller for the Superset fork"
requires-python = ">=3.11"
dependencies = ["requests==2.32.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
